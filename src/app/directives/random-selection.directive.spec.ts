import { RandomSelectionDirective } from './random-selection.directive';

describe('RandomSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
